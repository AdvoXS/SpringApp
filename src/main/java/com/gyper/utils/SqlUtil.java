package com.gyper.utils;

import java.util.HashMap;
import java.util.Map;

public class SqlUtil {
   /**
     * Преобразовывает мапу параметров для where
     * @param filterParams фильтры полей
     * @return фильтр-строка sql
     */
   /*
    public static String getWhereFilter(HashMap<String, Object> filterParams){
        StringBuilder filterBuilder = new StringBuilder();
        String predicate = null;
        for(Map.Entry<String, Object> entry : filterParams.entrySet()){
            predicate = entry.getKey();
            //entry.getValue().
        }
        return filterBuilder.toString();
    }*/
}
